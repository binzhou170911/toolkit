## ADDED Requirements

### Requirement: Visible selected game settings
The system SHALL visually distinguish the active game mode and active AI difficulty from inactive choices using a perceptible fill, border or ring, and text treatment.

#### Scenario: Active mode is visible
- **WHEN** a player selects a game mode or AI difficulty
- **THEN** the selected control SHALL remain visibly distinct from all inactive controls in the active theme
